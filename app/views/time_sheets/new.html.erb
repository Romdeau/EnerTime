<% @user = User.find(params[:user_id]) %>
<div class="page-header">
  <h1>Create Time Sheet for <%= format_name(@user.email)%></h1>
</div>

<div id="content">
  <div class="paragraph">
    <%= render 'form' %>
    <%= link_to 'Back', time_sheets_path %>
  </div>
</div>
