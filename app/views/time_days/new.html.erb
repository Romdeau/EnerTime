<% @time_sheet = TimeSheet.find(params[:time_sheet_id]) %>
<% @user = @time_sheet.user %>
<div class="page-header">
  <h1>New Time Sheet Day for <%= format_name(@user.email)%></h1>
</div>
<%= render 'form' %>

<%= link_to 'Back', time_days_path %>
